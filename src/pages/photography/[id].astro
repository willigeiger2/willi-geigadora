---
import PhotoLightboxLayout from '../../layouts/PhotoLightboxLayout.astro';

const { title, id } = Astro.params;
export const prerender = false;
---

<PhotoLightboxLayout title={title as string} id={id as string}>
</PhotoLightboxLayout>
